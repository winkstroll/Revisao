#include <stdio.h>
#include <stdlib.h>

main (){
int idade;
float peso;
char nome[30];

printf("Digite sua nome: ");
scanf ("%s", &nome);
printf("Digite sua idade: ");
scanf ("%d", &idade);
printf("Digite seu peso: ");
scanf("%f", &peso);

    if(idade<18){
    	printf("Você não pode doar sangue",nome);
    }
    if((idade>=18)) && ((peso<50)){
    	printf("%sÉmaaior mas não tem peso suficiente");
    }
    if ((idade>=18)) && ((peso>=50)){
    	printf("%svocê pode doar sangue");
    }
